# Changelog

## 0.2.0 - 2026-04-05
- 以 v0.1.6 為基準整合 PC / EMU / CMD：設定頁重構為獨立分頁，PC/EMU 啟動頁加入分頁級 `儲存設定 / 重置變更 / 恢復預設`，並補上未儲存設定防呆與欄位級差異提示。
- 重構斷線重連與自動開啟流程：新增共用輔助模組、補上 `DisconnectHandler.refresh_config()`、修正登入後公告與彈窗處理、遊戲開啟檢查、低活力補充、AI/徘徊維護與多處時序問題。
- 優化執行體驗：PC/EMU 啟動器記憶視窗位置、Debug 搜尋日誌節流與批次寫入、執行中鎖定關鍵設定欄位，並改善說明頁、多語系與欄位提示一致性。
- 同步更新預設值與資源：調整 PC/EMU 多組等待時間與辨識閾值，EMU 預設 `活力值9時畫面` 更新為 `0.95`，新增與替換登入/公告/戰鬥模板、更新圖示與打包配置。

## 0.1.6 - 2026-03-29
- README 改為精簡版，完整歷史移至 `CHANGELOG.md`。
- 新增獨立的 PC / EMU PyInstaller spec，改用 `launcher.py` 與 `launcher_emu.py` 作為打包入口。
- 補上 `pywin32` 相依與 PyInstaller hidden imports，確保 `win32gui` / `win32ui` 與 `PyQt5.sip` 可正確打包。
- 保持既有熱更新流程：恢復時與儲存後執行中皆會推送 `RUNNING_CONFIG`，並立即刷新 bot 執行期設定。

## 0.2.20 - 2026-04-05
- 修正執行中「暫停 -> 儲存設定 -> 恢復」時新設定未立即生效：恢復執行時改為固定推送最新設定到 `RUNNING_CONFIG`（EMU/PC 同步）。
- 修正執行中按下「儲存設定」後的即時同步：儲存完成後若仍在執行中，立即推送設定並刷新 bot 執行期參數（EMU/PC 同步）。

## 0.2.19 - 2026-04-05
- 調整 EMU「自動開啟功能」分頁容器結構，改為與「斷線重連」一致的外層捲動佈局與間距設定，修正外框內縮不一致問題。
- 修正 EMU 設定頁語系刷新：補上 `EMU 套件名稱` GroupBox 與欄位標籤於 `update_ui_texts()` 的多語系更新。
- 新增 `original_value_label` 多語系鍵值（繁中/簡中/English），並套用於欄位「原值」標記，避免硬編碼中文。

## 0.2.18 - 2026-04-05
- 修正 GUI 啟動匯入錯誤：將 `QFont` 從 `PyQt5.QtWidgets` 的錯誤匯入改為僅使用 `PyQt5.QtGui`，避免 `launcher_emu.py` 啟動時拋出 `ImportError`。
- 同步修正 `gui/pc_ui.py` 相同匯入問題，避免 PC 介面啟動時發生同型錯誤。
- 確認專案內無 `autoPVP` 代碼或相容別名；目前核心模組僅為 `autoPVE`。

## 0.2.17 - 2026-04-05
- 調整 EMU「自動開啟功能」頁版面：主內容區塊加長並改為可捲動，降低欄位擁擠感。
- 調整 EMU「斷線重連」頁上方留白：移除無內容表單區塊，讓「最大重連次數」區域上移。
- 調整 EMU「自動開啟功能 > 等待時間設定」為單列三欄（自動開啟點擊等待時間 / 自動開啟辨識間隔 / 遊戲內確認逾時）。
- 調整 EMU「自動開啟功能」區塊順序：將「每台設備自動開啟功能」移至「辨識閾值設定（原遊戲內恢復）」下方。
- 調整 EMU 文案：
	- 「遊戲內恢復」改為「辨識閾值設定」
	- 「斷線重連辨識閾值設定」改為「辨識閾值設定」
- 調整 EMU 欄位位置：將「同畫面辨識差異閾值」移入「辨識閾值設定」區塊中。

## 0.2.16 - 2026-04-07
- 重構 `launcher_emu.py`（2292 行）與 `launcher.py`（1718 行）：將所有 UI 邏輯拆分至 `gui/` 子套件（14 個模組）。
- 新增 `gui/` 套件：`shared.py`、`emu_threads.py`、`emu_log.py`、`emu_display.py`、`emu_config.py`、`emu_control.py`、`emu_ui.py`（EMU 側）及 `pc_threads.py`、`pc_log.py`、`pc_display.py`、`pc_config.py`、`pc_control.py`、`pc_ui.py`（PC 側）。
- `launcher_emu.py` 與 `launcher.py` 精簡為薄型入口（各約 100 / 60 行），僅保留 `__init__` 初始化與 `main()` 進入點。
- 修正 `start_gui.bat` 視窗標題，由舊標題更新為「女王的飄流小助手」。

## 0.2.15 - 2026-04-05
- 修正欄位原值提示殘留：按下「儲存設定」後，欄位標籤/勾選文字不再保留 `(原: ...)` 標記。
- 修正「恢復預設」套用來源：EMU 模式會正確以 `default_config_emu.json` 的預設值全量回填（含 `thresholds.EMU` 與 `disconnect`）。
- 調整「自動開啟功能」頁版面：改為主開關置頂，並在其下方新增灰底外框容器，符合斷線重連頁的視覺層次。

## 0.2.14 - 2026-04-05
- 調整 EMU「斷線重連」頁面外框：移除「斷線重連」大外框，讓內容直接呈現。
- 調整「同畫面偵測設定」區塊：移除外框，並保留 `最大重連次數 + 同畫面辨識差異閾值` 同列顯示（左最大重連次數、右同畫面辨識差異閾值）。
- 更新欄位名稱：`辨識差異閾值` 改為 `同畫面辨識差異閾值`（繁中/簡中/English 同步）。
- 修正啟動後誤顯示「有未儲存設定」：新增初始化階段保護，UI 建立期間不會把設定標記為已變更。
- 調整「自動開啟功能」頁籤版面：新增與其他頁一致的大框區塊與灰底視覺，提升三頁籤一致性。

## 0.2.13 - 2026-04-05
- 修正 EMU 啟動時崩潰：`create_config_tab()` 期間觸發 `on_config_changed()`，但 `start_btn` 尚未建立，導致 `_update_run_action_guard()` 拋出 `AttributeError`。
- 在 `_update_run_action_guard()` 新增初始化保護，於控制列元件尚未建立時直接返回，待 UI 完整建立後再更新狀態。

## 0.2.12 - 2026-04-05
- 調整 EMU「斷線重連」版面：將 `同畫面逾時`、`重啟等待秒數`、`同畫面偵測間隔` 移入「等待時間設定」區塊，集中管理重連流程等待參數。
- 調整「同畫面偵測設定」排版：`最大重連次數` 與 `同畫面辨識差異閾值` 改為同一行顯示，左半為最大重連次數、右半為辨識差異閾值。
- 同步更新欄位標籤刷新、欄位說明 tooltip 與紅字差異提示邏輯，確保版面搬移後仍可正確顯示原值比對。

## 0.2.11 - 2026-04-05
- 補齊 EMU 設定頁三分頁欄位級差異提示：移除「目前套用設定」區塊後，改由紅字標示變更欄位，並在欄位標籤/勾選文字附註原始值（以載入/上次儲存為基準）。
- 補上「自動開啟點擊等待時間」多語系鍵值與欄位說明，三語系（繁中/簡中/English）同步。
- 強化設定鎖定規則：關閉「啟用斷線重連」時不可修改「最大重連次數」；關閉「啟用自動開啟功能」時不可修改每台設備的自動開啟勾選。
- 調整說明文案，避免在主要說明頁中要求使用者理解 A/B/C 流程代號，改為功能導向描述。
- 設定操作按鈕調整為三鍵：`儲存設定` / `重置操作`（回復到變更前）/ `恢復預設`（僅將已變更項目改回預設）。
- 新增未儲存變更防呆：若有未儲存設定，禁止「啟動」與「繼續」，並於狀態列提示先儲存或重置。

## 0.2.10 - 2026-04-05
- 移除 EMU 設定頁三個分頁中的「目前套用的設定」文字區塊，改為欄位級差異提示：有變更的欄位/勾選項會顯示紅色，並在欄位標籤或勾選文字附註原始值。
- 變更比對基準改為「載入設定或最近一次保存後的值」；保存成功後會重設基準並清除紅字標示。
- 調整「啟用自動開啟功能」主開關樣式：套用與「啟用斷線重連」一致的大字體與底色，提升頁籤一致性與可讀性。

## 0.2.9 - 2026-04-05
- 修正 `DisconnectHandler.check()`：當斷線重連關閉但自動開啟功能啟用時，仍會被動同步遊戲 `game_state`，並在 `STATE-4` 執行自動開啟維護；避免狀態停在 `DISCONNECT` 導致不偵測。
- 調整 `STATE-4` 斷線判斷門檻：斷線重連關閉時不觸發斷線流程切換，僅保留遊戲內自動開啟功能維護。
- 調整 EMU 自動開啟功能頁 UI：移除「斷線重連辨識閾值設定」外框，保留「遊戲內恢復」獨立區塊；同時將「啟用自動開啟功能」改為 GroupBox 樣式，與其他頁籤一致。

## 0.2.8 - 2026-04-05
- 調整 EMU「自動開啟功能」頁籤：將 `自動開啟辨識間隔` 與 `遊戲內確認逾時` 自斷線重連頁移入，並新增獨立的 `自動開啟點擊等待時間`，讓 Flow-C / STATE-4 與斷線重連流程使用不同的操作冷卻。
- 調整 `DisconnectHandler`：自動開啟功能的 AI / 徘徊點擊與重試冷卻改用 `auto_feature_action_cooldown`，不再共用斷線重連的 `action_cooldown`。
- 調整 EMU 斷線重連閾值區塊排版：每列最多顯示 4 個欄位，縮短整體頁面長度；並加大自動開啟功能頁的「目前套用的設定」顯示區高度。

## 0.2.7 - 2026-04-05
- 補正 EMU 設定頁的「自動開啟功能」分頁：將「遊戲內恢復」辨識閾值區塊自斷線重連分頁移入獨立頁籤，避免 UI 拆分後仍殘留在原位置。
- 補正「目前套用的設定」顯示：新增自動開啟功能頁專屬的目前設定顯示區，集中呈現 Flow-C 開關、每台設備自動開啟設定，以及遊戲內恢復辨識閾值；斷線重連頁則只保留 A/B 與重連本身相關資訊。
- 調整 enable/disable 邏輯：遊戲內恢復相關辨識閾值改由自動開啟功能主開關控制，不再跟隨斷線重連主開關一起停用。

## 0.2.6 - 2026-04-04
- 修正 Flow-B 進入後立即偵測到 `btn_power_saving` 誤判「已在遊戲內」：在進入流程後前 2 秒跳過 `btn_power_saving` 檢查，等待遊戲畫面穩定後再判斷。
- 移除設定頁籤中「重開遊戲」與「登入遊戲」子分頁：相關後端功能（流程 A/B 開關）保留為隱藏元件，後續可擴充；UI 不再顯示以簡化介面。
- 新增獨立頂層「自動開啟功能」設定分頁（STATE-4 / 流程 C）：移出原斷線重連分頁的子分頁，改為與「自動玩家對戰」和「斷線重連」並列的獨立分頁，並移除對斷線重連主開關的依賴。

## 0.2.5 - 2026-04-04
- 調整 `btn_ai_off_in_battle` 分數日誌回 Debug-only：非 Debug 模式不再輸出 `[SEARCH]` 分數。
- 補強戰鬥中 Debug 探針：在 `STATE-4` 且 `in_battle` 時固定探測一次 `btn_ai_off_in_battle`，確保 Debug 模式可穩定看到分數日誌。

## 0.2.4 - 2026-04-04
- 新增 `btn_ai_off_in_battle` 模板偵測分數日誌：即使關閉 Debug 模式，仍會輸出 `[SEARCH]` 命中/未命中、分數、門檻與耗時，方便定位 AI 按鈕誤判與閾值調校。

## 0.2.3 - 2026-04-04
- 調整 STATE-4 戰鬥中 AI 邏輯為持續監看模式：不再使用「本場戰鬥僅一次」跳過策略，改為持續偵測 `btn_ai_off_in_battle`。
- 新增 AI 防抖與重試冷卻：需連續命中 2 幀才嘗試點擊，且兩次點擊之間套用冷卻，避免偵測抖動造成 AI 持續切換。
- 同步調整 Flow-C 的 `enable_ai`：改為持續監看 + 防抖 + 冷卻，當戰鬥中未偵測到 AI 關閉按鈕即視為 AI 已開啟並恢復主流程。
- 調整 Flow-C 的 `enable_lurk`：戰鬥中一律略過徘徊步驟，不再送出 R 快捷鍵，避免戰鬥中快捷鍵映射造成 AI 誤切換。

## 0.2.2 - 2026-04-04
- 修正 STATE-4 戰鬥中 AI 開啟旗標（`_state4_ai_battle_handled`）因單幀 `in_battle` 偵測跳變而被意外重設，導致 AI 被反覆嘗試切換（持續切換狀態）；改以連續 5 幀非戰鬥判定才重設旗標。
- 移除 STATE-4 戰鬥中以 R 熱鍵執行徘徊的邏輯：R 鍵在部分遊戲戰鬥中為 AI 開關，誤送 R 鍵會造成 AI 狀態被意外切換；徘徊改為僅在非戰鬥的一般地圖上透過模板偵測開啟。
- 修正 Flow-C `enable_ai` 步驟：戰鬥中 AI 已開啟後陷入無窮等待迴圈（永遠不呼叫 `_finish_recovery`）；現在改為立即結束流程，恢復主循環。

## 0.2.1 - 2026-04-04
- 修正斷線入口流程：偵測到 `btn_confirm` 後不再停留於入口判斷，改為直接銜接流程 C（進遊戲後檢查），避免誤觸發「入口判斷逾時」回登入。
- 修正執行中設定刷新錯誤：補上 `DisconnectHandler.refresh_config()` 對外方法，解決 `disconnectHandler object has no attribute 'refresh_config'`。
- 修正自動徘徊與戰鬥 AI 互相干擾：戰鬥中優先處理 AI 開啟檢查，當 AI 自動檢查啟用時略過徘徊快捷鍵，避免重複點擊 AI 按鈕。

## 0.2.0 - 2026-04-03
- 重構斷線重連核心流程（A/B/C）並抽離共用模組 `core/disconnect_helpers.py`，提升流程可維護性與一致性。
- 強化重連穩定性與可觀測性：補強登入後彈窗處理、同畫面/遊戲狀態判斷、流程節流與關鍵日誌輸出。
- 新增並整併完整模板資源集（含 `update_resource`、登入流程與戰鬥相關模板），移除舊版 `ref_*` 模板檔。
- 更新圖示資源：替換 `app.ico`，同步作為本次打包版本圖示來源。
- 調整 PC / EMU 設定項與預設值（含斷線重連、檢查間隔、閾值與自動功能相關配置），並同步多語系說明文字。
- 優化 GUI 與執行期行為：日誌篩選與輸出處理更穩定、執行中設定保護與狀態顯示更一致。
- PC 模式偵測到單一視窗時自動勾選，省去手動選取步驟。
- 除錯模式下狀態列顯示目前 `game_state`（DISCONNECT / SELECT_CHARACTER / IN_GAME / AFTER_IN_GAME），PC 與 EMU 共同適用。